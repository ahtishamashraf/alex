'use client';import {useState} from 'react';import {listings} from '@/data/listings';import ListingCard from './ListingCard';import {motion,AnimatePresence} from 'framer-motion';
export default function ListingsGrid(){const [filter,setFilter]=useState('All');const tabs=['All','Dubai / UAE','United Kingdom','Villa','Penthouse'];const shown=filter==='All'?listings:listings.filter(x=>x.market===filter||x.type===filter);return <><div className="flex flex-wrap gap-2 mb-12">{tabs.map(tab=><button onClick={()=>setFilter(tab)} key={tab} className={`px-5 py-3 border text-[10px] uppercase tracking-widest transition ${filter===tab?'bg-champagne text-black border-champagne':'border-white/15 text-white/60 hover:border-white/50'}`}>{tab}</button>)}</div><motion.div layout className="grid lg:grid-cols-2 gap-7"><AnimatePresence>{shown.map(item=><motion.div layout initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} exit={{opacity:0}} key={item.slug}><ListingCard item={item}/></motion.div>)}</AnimatePresence></motion.div></>}
